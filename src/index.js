import React from "react";
import ReactDOM from "react-dom/client";
import DesiSite from "./DesiSite";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DesiSite />
  </React.StrictMode>
);